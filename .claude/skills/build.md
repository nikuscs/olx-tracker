# Build Project

Build the olx-tracker project.

## Usage
Run when you need to compile the project.

## Commands

```bash
# Debug build (faster, larger binary)
cargo build

# Release build (optimized, smaller binary)
cargo build --release
```

## Output
- Debug: `target/debug/olx-tracker`
- Release: `target/release/olx-tracker`
